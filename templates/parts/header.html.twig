

  
<header>
<nav class="navbar navbar-dark bg-dark">
  <a class="navbar-brand" href="#">La Gazette HB</a>
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="{{ path('article_index') }}"> Voir tous les articles <span class="sr-only">(current)</span></a>
      </li>
    </ul>
  {% if app.user %}
  <p class="welcome_message"> Bonjour {{ app.user.username}}</p> 
  {% else %}  
  <a href="{{path('app_login')}}">se connecter</a>  
  {% endif %}
</nav>
</header>


